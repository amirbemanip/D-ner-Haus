# مستندات جامع پروژه Dönerhaus

این مستند شامل توضیحات کامل و با جزئیات دقیق از وب‌سایت Dönerhaus است؛ از ظاهر و تجربه کاربری گرفته تا ساختار کدها و منطق بیزنس.

---

## ۱. هویت بصری و طراحی (UI/UX)

سایت Dönerhaus با تم **"Masterpiece"** طراحی شده است که حس لوکس و خاص بودن (Elite) را به مشتری منتقل می‌کند.

*   **پالت رنگی:** استفاده از مشکی متالیک (Obsidian Black - `#030303`) به عنوان رنگ پایه و نارنجی/طلایی برند (`#FF6B00`) برای تاکیدها و جزئیات.
*   **تایپوگرافی:**
    *   **Syncopate:** برای عناوین و برندینگ (حسی مدرن و صنعتی).
    *   **Outfit:** برای متون بدنه و رابط کاربری (خوانایی بالا و استایل تمیز).
*   **جلوه‌های بصری و انیمیشن‌ها:**
    *   **GSAP:** برای انیمیشن‌های پیچیده مانند ظاهر شدن بخش‌ها هنگام اسکرول (Reveal)، شناور بودن کارت‌ها (Floating) و حرکت لگو در هدر.
    *   **Framer Motion:** برای جابه‌جایی‌های نرم بین صفحات و حالت‌های مختلف المان‌ها.
    *   **Lenis:** برای تجربه اسکرول بسیار نرم (Smooth Scroll).
    *   **Glassmorphism:** استفاده از پنل‌های شیشه‌ای با پس‌زمینه مات (Backdrop Blur) و حاشیه‌های ظریف.
    *   **Liquid Glass Bar:** نوار درخشان متحرک زیر نام برند در نوار ناوبری.
    *   **Custom Cursor:** نشانگر موس اختصاصی که در نسخه‌های دسکتاپ تجربه تعاملی را تقویت می‌کند.

---

## ۲. ویژگی‌ها و قابلیت‌ها (Features)

پروژه به سه بخش اصلی تقسیم می‌شود: پنل مشتری، پنل فروشنده و پنل مدیریت.

### الف) بخش مشتری (Customer)
*   **ثبت‌نام:** مشتری با نام و شماره تلفن ثبت‌نام کرده و یک کد ۶ رقمی اختصاصی دریافت می‌کند.
*   **کارت وفاداری دیجیتال:** نمایش یک کارت شیک شامل نام، کد عضویت، QR Code و تعداد استمپل‌ها (مهرها).
*   **سیستم وفاداری (Loyalty):**
    *   هر ۱۰ خرید = ۱ دونر رایگان.
    *   هدیه خوش‌آمدگویی: سیب‌زمینی سرخ‌کرده رایگان برای اولین خرید.
*   **امکانات کارت:**
    *   ذخیره کارت به صورت تصویر (PNG).
    *   افزودن به Apple Wallet.
    *   چاپ مستقیم کارت.
*   **Stempel-Check:** نوار جستجوی سریع در بالای صفحه برای چک کردن وضعیت امتیازها بدون نیاز به لاگین کامل.
*   **امتیاز گوگل مپ:** مشتری می‌تواند با ثبت نظر در گوگل مپ و تایید توسط ادمین، ۱ امتیاز (استمپل) هدیه بگیرد.

### ب) پنل فروشنده (Seller POS)
*   **دسترسی محافظت شده:** ورود با رمز عبور مخصوص.
*   **جستجوی مشتری:** از طریق کد عضویت یا شماره تلفن.
*   **اسکنر QR:** قابلیت باز کردن دوربین و اسکن مستقیم کد عضویت مشتری.
*   **مدیریت خرید:** دکمه "Add Stamp" برای اضافه کردن امتیاز خرید و دکمه‌های "Redeem" برای استفاده از جوایز (دونر یا سیب‌زمینی رایگان).

### ج) پنل مدیریت (Admin Dashboard)
*   **آمار زنده:** نمایش تعداد اعضا، مجموع استمپل‌های فعال و هدایای دریافت شده.
*   **لیست اعضا:** مشاهده و فیلتر کردن تمامی مشتریان ثبت‌نام شده.
*   **مرکز پیام (Broadcast Center):** ارسال پیام‌های هدفمند به تمام اعضا یا یک عضو خاص (بر اساس شماره یا کد).
*   **تایید نظرات گوگل:** بررسی درخواست‌های امتیاز هدیه و تایید یا رد آن‌ها.

---

## ۳. ساختار فنی و کدها (Technical Stack)

این وب‌سایت با استفاده از مدرن‌ترین ابزارهای دنیای وب توسعه یافته است:

*   **Frontend:**
    *   **Next.js 15 (App Router):** فریم‌ورک اصلی برای رندرینگ سریع و بهینه.
    *   **React 19:** کتابخانه اصلی رابط کاربری.
    *   **Tailwind CSS v4:** برای طراحی واکنش‌گرا و استایل‌دهی سریع.
    *   **TypeScript:** برای امنیت کد و جلوگیری از خطاهای تایپی.
*   **Backend:**
    *   **Next.js Route Handlers:** برای ایجاد API های پروژه.
    *   **Prisma ORM:** برای مدیریت ارتباط با پایگاه داده.
*   **Database:**
    *   **PostgreSQL:** پایگاه داده اصلی برای ذخیره اطلاعات مشتریان و پیام‌ها.
    *   **Demo Mode:** یک سیستم هوشمند در `src/lib/prisma.ts` که اگر دیتابیس وصل نباشد، اطلاعات را در حافظه موقت (In-memory) نگه می‌دارد تا سایت همچنان قابل تست باشد.
*   **امنیت:**
    *   **Middleware:** لایه امنیتی برای محافظت از مسیرهای `/admin` و `/seller`.
    *   **Cookie Auth:** استفاده از کوکی برای حفظ دسترسی مدیر و فروشنده.
*   **کتابخانه‌های جانبی:**
    *   `html-to-image`: برای تبدیل کارت عضویت به عکس.
    *   `html5-qrcode`: برای سیستم اسکنر بارکد.
    *   `lucide-react`: برای آیکون‌های زیبا و یکدست.

---

## ۴. منطق برنامه (Logic)

*   **تولید کد عضویت:** هنگام ثبت‌نام، یک کد ۶ رقمی رندوم و منحصر به فرد تولید می‌شود.
*   **محاسبه امتیاز:** در دیتابیس فیلد `coupons` تعداد امتیازها را نگه می‌دارد. وقتی به ۱۰ برسد، دکمه جایزه در پنل فروشنده فعال می‌شود.
*   **ارسال پیام (Broadcast):** سیستم پیام‌ها را فیلتر می‌کند؛ مشتری فقط پیام‌هایی را می‌بیند که یا برای "همه" ارسال شده یا دقیقا برای شماره تلفن/کد او باشد.

این پروژه ترکیبی از هنر طراحی و مهندسی نرم‌افزار است تا تجربه خرید دونر را به یک سطح بالاتر (Premium) ارتقا دهد.
